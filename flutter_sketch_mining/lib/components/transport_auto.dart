
import 'package:flutter/material.dart';

class TransportAuto extends StatelessWidget {

  @override
  Widget build(BuildContext? context) {
    return Container(
      color: Colors.indigo.shade50,
      child: const Center(
        child: Text('Auto Test Not Ready ATM')
      ),
    );
  }
}